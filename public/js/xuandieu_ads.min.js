//Importance Code
window.jQuery || document.write('<script src="./js/vendor/jquery.min.js"><\/script>')
// Using for particular city
function openCity(evt, cityName) {
    // Declare all variables
    var i, tabcitycontent, tabcity;

    // Get all elements with class="tabcontent" and hide them
    tabcitycontent = document.getElementsByClassName("tabcitycontent");
    for (i = 0; i < tabcitycontent.length; i++) {
        tabcitycontent[i].style.display = "none";
    }

    // Get all elements with class="tablinks" and remove the class "active"
    tabcity = document.getElementsByClassName("tabcity");
    for (i = 0; i < tabcity.length; i++) {
        tabcity[i].className = tabcity[i].className.replace(" active", "");
    }

    // Show the current tab, and add an "active" class to the button that opened the tab
    document.getElementById(cityName).style.display = "block";
    evt.currentTarget.className += " active";
}
// Using for Hot new and Online Salling
function openSale(evt, cityName) {
    // Declare all variables
    var i, tabcontent, tablinks;

    // Get all elements with class="tabcontent" and hide them
    tabcontent = document.getElementsByClassName("tabsalecontent");
    for (i = 0; i < tabcontent.length; i++) {
        tabcontent[i].style.display = "none";
    }

    // Get all elements with class="tablinks" and remove the class "active"
    tablinks = document.getElementsByClassName("tabsale");
    for (i = 0; i < tablinks.length; i++) {
        tablinks[i].className = tablinks[i].className.replace(" active", "");
    }

    // Show the current tab, and add an "active" class to the button that opened the tab
    document.getElementById(cityName).style.display = "block";
    evt.currentTarget.className += " active";
}
document.getElementById("defaultSale").click();
document.getElementById("defaultCity").click();